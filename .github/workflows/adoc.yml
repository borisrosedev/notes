name: adoc-workflow
run-name: ${{ github.actor }} adoc-workflow
on:
    push:
        paths:
            - '**.adoc'
          
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: adoc test
              run: echo "test"